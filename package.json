{
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2"
  },
  "lint-staged": {
    "app/src/**/*.{ts,tsx}": [
      "eslint --config app/eslint.config.js --fix --max-warnings=0",
      "prettier --write"
    ],
    "app/src/**/*.css": [
      "prettier --write"
    ]
  }
}
